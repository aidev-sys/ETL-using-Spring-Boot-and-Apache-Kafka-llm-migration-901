# Auto-generated by Spring Boot Universal Migrator
name: Build & Test Migrated Project

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      rabbitmq:
        image: rabbitmq:3-management
        ports:
          - 5672:5672
          - 15672:15672
        env:
          RABBITMQ_DEFAULT_USER: guest
          RABBITMQ_DEFAULT_PASS: guest
        options: >-
          --health-cmd "rabbitmqctl status"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      postgres:
        image: postgres:16
        ports:
          - 5432:5432
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: testdb
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build all modules
        run: |
          echo "=== Building ExtractingData ==="
          cd "ExtractingData"
          mvn -B compile -DskipTests --no-transfer-progress
          cd $GITHUB_WORKSPACE
          echo "=== Building LoadingData ==="
          cd "LoadingData"
          mvn -B compile -DskipTests --no-transfer-progress
          cd $GITHUB_WORKSPACE
          echo "=== Building TransformingData ==="
          cd "TransformingData"
          mvn -B compile -DskipTests --no-transfer-progress
          cd $GITHUB_WORKSPACE
        env:
          SPRING_RABBITMQ_HOST: localhost
          SPRING_RABBITMQ_PORT: 5672
          SPRING_RABBITMQ_USERNAME: guest
          SPRING_RABBITMQ_PASSWORD: guest
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/testdb
          SPRING_DATASOURCE_USERNAME: postgres
          SPRING_DATASOURCE_PASSWORD: postgres
          SPRING_JPA_HIBERNATE_DDL_AUTO: update
          SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect

      - name: Test all modules
        run: |
          echo "=== Testing ExtractingData ==="
          cd "ExtractingData"
          mvn -B test -DskipTests --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "=== Testing LoadingData ==="
          cd "LoadingData"
          mvn -B test -DskipTests --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          echo "=== Testing TransformingData ==="
          cd "TransformingData"
          mvn -B test -DskipTests --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
        continue-on-error: true
        env:
          SPRING_RABBITMQ_HOST: localhost
          SPRING_RABBITMQ_PORT: 5672
          SPRING_RABBITMQ_USERNAME: guest
          SPRING_RABBITMQ_PASSWORD: guest
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/testdb
          SPRING_DATASOURCE_USERNAME: postgres
          SPRING_DATASOURCE_PASSWORD: postgres
          SPRING_JPA_HIBERNATE_DDL_AUTO: update
          SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect

      - name: Package all modules
        run: |
          cd "ExtractingData"
          mvn -B package -DskipTests --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          cd "LoadingData"
          mvn -B package -DskipTests --no-transfer-progress || true
          cd $GITHUB_WORKSPACE
          cd "TransformingData"
          mvn -B package -DskipTests --no-transfer-progress || true
          cd $GITHUB_WORKSPACE

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build-output
          path: |
            target/*.jar
            **/target/*.jar
          retention-days: 7

      - name: Upload test reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-reports
          path: |
            target/surefire-reports/
            **/target/surefire-reports/
          retention-days: 7
